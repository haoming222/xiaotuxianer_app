================================================================================
商品详情页底部操作栏 — 开发变更日志
项目：uni-app（D:\uniappproject\app）
记录日期：2026-04-12
================================================================================

[2026-04-12 13:22:00] 任务说明
--------------------------------------------------------------------------------
需求：在「商品详情」页面底部增加类似手机淘宝的固定操作栏，包含：
  - 客服（图标 + 文案）
  - 收藏（图标 + 文案）
  - 加入购物车（主按钮样式）
  - 领券购买（渐变主按钮样式）
业务逻辑可暂不实现；图标需统一存放；风格与现有详情（灰底、价格区 #35c8a9 等）协调；
本次变更需在 log 目录以 txt 记录时间与详细说明。

[2026-04-12 13:22:15] 新增文件
--------------------------------------------------------------------------------
1) static/icons/detail-action/ic-customer-service.svg
   - 客服：线框耳机/客服图形，描边色 #555555，与详情页文字层次（#333/#555）一致。
2) static/icons/detail-action/ic-favorite.svg
   - 收藏：线框心形，未选中态，同色系描边。
说明：图标采用与 iconfont（阿里巴巴矢量图标库，https://www.iconfont.cn/ ）常见的线框电商图标一致的手绘 SVG，
      便于后续若从 iconfont 下载项目包，可直接替换同路径文件名或改 DetailActionBar 中的 src。
3) pages/detail/components/DetailActionBar/DetailActionBar.vue
   - 固定定位底部栏（position: fixed; bottom: 0; z-index: 1000）。
   - 适配安全区：padding-bottom 使用 constant(safe-area-inset-bottom) / env(safe-area-inset-bottom)。
   - 左侧双入口：客服、收藏，使用 image + text。
   - 右侧双按钮：「加入购物车」白底橙边 #ff5000；「领券购买」橙到红渐变（#ff9500 → #ff5000），贴近淘宝详情常见配色。
   - 交互：四个入口暂时统一使用 uni.showToast 提示「xxx（待接入）」，便于后续替换为跳转客服页、收藏接口、加购、领券结算等。

[2026-04-12 13:23:00] 修改文件
--------------------------------------------------------------------------------
1) pages/detail/detail.vue
   - 根节点由单一 scroll-view 改为外层 view.detail-page 包裹。
   - scroll-view 增加 scroll-y、:show-scrollbar="false"，避免底部内容被遮挡。
   - 增加底部内边距：padding-bottom: calc(3.75rem + safe-area)，与固定栏高度大致匹配。
   - 引入并挂载 <DetailActionBar />。
   - 样式：新增 .detail-page（min-height: 100vh、背景与原先详情灰底一致）。

[2026-04-12 13:23:30] 设计说明（与现有风格对齐）
--------------------------------------------------------------------------------
- 页面背景仍为 rgb(244 244 244)，与原先 .detail 一致。
- 价格卡片使用 #35c8a9，底部栏为白底 + 浅分割线 #eaeaea，不强行把整栏做成绿色，避免与淘宝式「双主按钮」冲突；
  若需与价格区更强统一，可将「加入购物车」描边色改为接近 #35c8a9（当前按「淘宝类似」保留橙红促销色）。

[2026-04-12 13:24:00] 后续可接入点（未实现）
--------------------------------------------------------------------------------
- onService：跳转客服页或打开企业微信/在线客服 SDK。
- onFavorite：调用收藏接口，切换 ic-favorite 为实心态或本地状态。
- onAddCart：打开规格弹窗（可与 SelectBox 联动）后请求加购接口。
- onCouponBuy：领券逻辑 + 跳转结算或唤起优惠券弹层。

[2026-04-12 13:24:15] 自检
--------------------------------------------------------------------------------
- 组件路径：./components/DetailActionBar/DetailActionBar.vue，与 DetailSwiper 等并列。
- 静态资源路径：/static/icons/detail-action/*.svg，与项目内其它 static 引用方式一致。

[2026-04-12 13:25:00] 补充
--------------------------------------------------------------------------------
- ic-customer-service.svg 若编辑器写入异常，已通过本机脚本写入 static/icons/detail-action/ 目录。
- DetailActionBar 按钮区间距使用 margin-right 替代 flex gap，以兼容部分小程序端对 gap 的支持差异。

================================================================================
日志结束
================================================================================
